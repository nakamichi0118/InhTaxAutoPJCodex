"""Prompt for generic document OCR - extracts structured table data from any document."""

GENERIC_OCR_PROMPT = """あなたは日本語の書類を構造化データに変換するAIアシスタントです。
添付PDFの内容を読み取り、表形式のデータがあればそれを構造化JSONとして抽出してください。

以下のJSON形式で返してください:

```json
{
  "document_title": "書類のタイトル（推定）",
  "headers": ["列名1", "列名2", "列名3", ...],
  "rows": [
    ["値1", "値2", "値3", ...],
    ["値1", "値2", "値3", ...]
  ],
  "metadata": {
    "顧客名": "...",
    "期間": "...",
    "その他": "..."
  }
}
```

注意事項:
- このページの全データを漏れなく抽出すること（1行も飛ばさない）
- 表形式データがある場合はheaders + rowsとして構造化すること
- 表が無い場合はheadersを空配列、rowsにテキスト行を1要素配列で入れること
- 日付はそのまま（変換不要）
- 金額のカンマは除去して数値文字列で返すこと
- metadataにはヘッダー部分の情報（顧客名、期間、支店名等）を入れること
- JSONのみ返すこと（説明不要）
"""
